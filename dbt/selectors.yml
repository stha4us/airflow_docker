selectors:
  - name: exclude_specific_models
    description: "Runs all models except selected ones with these tags."
    definition:
      union:
        - method: tag
          value: test
        - method: tag
          value: static_files  # union of same is fine
          children: true
      exclude:
        - method: tag
          value: legacy